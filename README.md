# Grounded RAG Policy Assistant

Span-grounded retrieval-augmented generation over an employee expense policy, running
entirely on local models.

Full documentation lands as the system is built. See `docs/superpowers/specs/` for the design.
